export enum USER_STATUS {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum TICKET_STATUS {
    OPEN = 'open',
    CLOSE = 'closed',
    SEEN = 'seen',
    REROLL = 'resolved',
}

export enum CV_STATUS {
    NEW = 'new',
    REJECT = 'reject',
    APPROVE = 'approve',
}

export enum POST_STATUS {
    PUBLIC = 'public',
    HIDDEN = 'hidden',
}
